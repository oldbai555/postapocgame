# Godot客户端规划文档

## 📋 项目概述

**项目名称**: postapocgame（后启示录游戏）  
**客户端引擎**: Godot  
**开发语言**: C#  
**游戏类型**: 仿2D横版格斗DNF游戏  
**目标平台**: Windows、安卓、iOS  
**服务端架构**: Gateway + GameServer + DungeonServer  
**开发模式**: 个人开发

### 客户端架构

```
┌─────────────────────────────────────────┐
│         Godot客户端（C#）                 │
├─────────────────────────────────────────┤
│  1. 网络层（Network）                     │
│     - 连接Gateway                        │
│     - Protocol Buffer协议解析            │
│     - 消息发送/接收管理                   │
├─────────────────────────────────────────┤
│  2. 游戏场景层（Scene）                   │
│     - 场景管理（SceneManager）            │
│     - 实体管理（EntityManager）           │
│     - 场景渲染（2D横版视角）              │
│     - AOI视野管理                        │
├─────────────────────────────────────────┤
│  3. 游戏逻辑层（GameLogic）               │
│     - 玩家控制（PlayerController）        │
│     - 移动系统（MovementSystem）          │
│     - 战斗系统（CombatSystem）            │
│     - 技能系统（SkillSystem）             │
│     - 自动战斗AI（AutoCombatAI）          │
├─────────────────────────────────────────┤
│  4. UI层（UI）                           │
│     - 登录界面（LoginUI）                 │
│     - 角色选择界面（RoleSelectUI）        │
│     - 主界面（MainUI）                    │
│     - 背包界面（BagUI）                   │
│     - 装备界面（EquipUI）                 │
│     - 任务界面（QuestUI）                 │
│     - 技能界面（SkillUI）                 │
│     - 邮件界面（MailUI）                  │
│     - 商城界面（ShopUI）                  │
│     - 副本界面（DungeonUI）               │
│     - 设置界面（SettingsUI）              │
├─────────────────────────────────────────┤
│  5. 数据层（Data）                       │
│     - 配置表管理（ConfigManager）         │
│     - 本地数据存储（LocalStorage）        │
│     - 玩家数据缓存（PlayerDataCache）     │
├─────────────────────────────────────────┤
│  6. 工具层（Utils）                      │
│     - 协议生成工具（ProtoCodeGenerator）  │
│     - 资源管理（ResourceManager）         │
│     - 日志系统（Logger）                  │
│     - 工具函数（UtilityFunctions）        │
└─────────────────────────────────────────┘
```

---

## 📐 技术栈

### 核心技术

1. **游戏引擎**: Godot 4.x
   - 使用C#脚本开发
   - 2D渲染系统
   - 场景管理系统

2. **网络通信**: 
   - WebSocket/TCP连接
   - Protocol Buffer序列化
   - 消息队列管理

3. **协议定义**: 
   - 使用`proto/csproto/`中的协议定义
   - 客户端和服务端共享同一份协议（包括公共枚举，如任务目标类型 `QuestType`、任务大类 `QuestCategory`、战斗状态等）
   - 通过工具生成C#代码

4. **数据存储**: 
   - 本地配置文件（JSON）
   - 玩家设置缓存
   - 账号信息（加密存储）

### 依赖库

1. **Protocol Buffers**: Google.Protobuf（C#版本）
2. **网络库**: Godot内置WebSocket或自定义TCP
3. **JSON解析**: Godot内置JSON解析
4. **日志库**: Godot内置日志系统或自定义日志

---

## 📁 目录结构规划

```
client/
├── Assets/                      # 游戏资源
│   ├── Sprites/                 # 精灵图
│   ├── Animations/              # 动画
│   ├── UI/                      # UI资源
│   ├── Audio/                   # 音频资源
│   └── Configs/                 # 客户端配置表
├── Scripts/                     # C#脚本
│   ├── Network/                 # 网络层
│   │   ├── NetworkManager.cs
│   │   ├── ProtocolHandler.cs
│   │   ├── MessageSender.cs
│   │   └── MessageReceiver.cs
│   ├── Scene/                   # 场景层
│   │   ├── SceneManager.cs
│   │   ├── EntityManager.cs
│   │   ├── Entity.cs
│   │   ├── PlayerEntity.cs
│   │   ├── MonsterEntity.cs
│   │   └── NPCEntity.cs
│   ├── GameLogic/               # 游戏逻辑层
│   │   ├── PlayerController.cs
│   │   ├── MovementSystem.cs
│   │   ├── CombatSystem.cs
│   │   ├── SkillSystem.cs
│   │   └── AutoCombatAI.cs
│   ├── UI/                      # UI层
│   │   ├── LoginUI.cs
│   │   ├── RoleSelectUI.cs
│   │   ├── MainUI.cs
│   │   ├── BagUI.cs
│   │   ├── EquipUI.cs
│   │   ├── QuestUI.cs
│   │   ├── SkillUI.cs
│   │   ├── MailUI.cs
│   │   ├── ShopUI.cs
│   │   ├── DungeonUI.cs
│   │   └── SettingsUI.cs
│   ├── Data/                    # 数据层
│   │   ├── ConfigManager.cs
│   │   ├── LocalStorage.cs
│   │   └── PlayerDataCache.cs
│   ├── Utils/                   # 工具层
│   │   ├── Logger.cs
│   │   ├── ResourceManager.cs
│   │   └── UtilityFunctions.cs
│   └── Protocol/                # 协议层（自动生成）
│       ├── CS.cs                # 客户端到服务端协议
│       ├── SC.cs                # 服务端到客户端协议
│       └── Base.cs              # 基础数据结构
├── Scenes/                      # Godot场景文件
│   ├── Login.tscn
│   ├── RoleSelect.tscn
│   ├── Main.tscn
│   ├── Game.tscn
│   └── UI/
│       ├── BagUI.tscn
│       ├── EquipUI.tscn
│       └── ...
├── Proto/                       # Protocol Buffer定义（从proto/复制）
│   └── csproto/
└── README.md                    # 客户端说明文档
```

### ✅ 当前目录结构（2025-11-18）

> 说明：下列结构已按上述模板创建，空目录下放置 `.gitkeep` 以便版本管理。

- `Assets/`  
  - `Sprites/`（示例精灵：`ExamplePlayerSprite.png`）  
  - `Animations/`、`UI/`、`Audio/`、`Configs/`（已创建占位）
- `Scripts/`  
  - `Network/`（`NetworkManager.cs`、`ProtocolHandler.cs`、`MessageSender.cs`、`MessageReceiver.cs`）  
  - `Scene/`（已创建，等待 `SceneManager.cs` 等脚本）  
  - `GameLogic/`（已迁移 `Player.cs`，用于示例角色）  
  - `UI/`（`LoginUI.cs`、`RoleSelectUI.cs`）  
  - `Data/`（`LocalStorage.cs`）  
  - `Utils/`（`Logger.cs`）  
  - `Protocol/`（由 `proto/genproto_csharp.bat` 自动生成的协议代码）
- `Scenes/`  
  - `AutoLoad.tscn`（自动加载 NetworkManager、MessageReceiver）  
  - `Login.tscn`、`RoleSelect.tscn`  
  - `player.tscn`（示例角色场景，脚本/贴图已指向 GameLogic 与 Assets 目录）  
  - `UI/`（待添加 UI 场景）
- 根目录其他文件  
  - `playground.tscn`（测试场景，实例化 `Scenes/player.tscn`，可用于快速验证移动/脚本逻辑）  
  - `project.godot`、`client.csproj`、`client.sln` 等工程文件

> 旧的 `client/Player/` 目录已被移除，原有脚本/贴图/场景分别迁移到 `Scripts/GameLogic/`、`Assets/Sprites/`、`Scenes/`。  
> 如果需要新的示例资源，请直接放入上述标准目录。

---

## 🎮 核心模块规划

### 1. 网络层（Network）

#### 1.1 网络连接管理
- **功能**: 管理客户端到Gateway的连接
- **实现**:
  - WebSocket/TCP连接建立和维护
  - 心跳机制
  - 断线重连机制
  - 连接状态管理

#### 1.2 协议处理
- **功能**: Protocol Buffer协议解析和打包
- **实现**:
  - 协议消息序列化/反序列化
  - 消息ID映射
  - 消息队列管理
  - 协议消息分发

#### 1.3 消息发送/接收
- **功能**: 处理客户端到服务端的消息发送和接收
- **实现**:
  - 消息发送接口（C2S协议）
  - 消息接收处理（S2C协议）
  - 消息回调机制
  - 超时处理

**相关协议**:
- 客户端发送协议（C2S）: `C2SRegister`、`C2SLogin`、`C2SQueryRoles`等（共27个协议）
- 服务端返回协议（S2C）: `S2CRegisterResult`、`S2CLoginResult`、`S2CRoleList`等（共36个协议）

---

### 2. 游戏场景层（Scene）

#### 2.1 场景管理
- **功能**: 管理游戏场景的加载和切换
- **实现**:
  - 场景加载/卸载
  - 场景切换动画
  - 场景数据缓存
  - 场景配置读取

#### 2.2 实体管理
- **功能**: 管理场景中的实体（玩家、怪物、NPC、掉落物）
- **实现**:
  - 实体创建/销毁
  - 实体位置更新
  - 实体状态同步
  - AOI视野管理（实体进入/离开视野）

#### 2.3 场景渲染
- **功能**: 2D横版场景渲染
- **实现**:
  - 地图渲染（TileMap或ParallaxBackground）
  - 实体渲染（精灵动画）
  - 特效渲染（技能特效、受击特效）
  - UI渲染（血条、名字、伤害数字）

**相关协议**:
- `S2CEnterScene` - 进入场景
- `S2CEntityAppear` - 实体进入视野
- `S2CEntityDisappear` - 实体离开视野
- `S2CEntityMove` - 实体移动
- `S2CEntityStopMove` - 实体停止移动

---

### 3. 游戏逻辑层（GameLogic）

#### 3.1 玩家控制
- **功能**: 处理玩家输入和角色控制
- **实现**:
  - 输入处理（键盘/触屏）
  - 角色移动控制
  - 技能释放控制
  - 交互控制（拾取、对话等）

#### 3.2 移动系统
- **功能**: 处理玩家和实体移动
- **实现**:
  - 移动输入处理
  - 移动动画播放
  - 移动同步到服务端
  - 服务端移动数据接收和同步

**相关协议**:
- `C2SStartMove` - 开始移动
- `C2SUpdateMove` - 移动中更新
- `C2SEndMove` - 结束移动
- `S2CEntityMove` - 实体移动广播
- `S2CEntityStopMove` - 实体停止移动广播

#### 3.3 战斗系统
- **功能**: 处理战斗逻辑
- **实现**:
  - 技能释放逻辑
  - 伤害数字显示
  - 受击动画播放
  - 战斗状态管理（与服务端的战斗状态枚举保持一致）
  - Buff/Debuff显示

**相关协议**:
- `C2SUseSkill` - 使用技能
- `S2CSkillCastResult` - 技能释放结果
- `C2SGetNearestMonster` - 获取最近怪物

#### 3.4 技能系统
- **功能**: 管理玩家技能
- **实现**:
  - 技能列表管理
  - 技能CD显示
  - 技能图标显示
  - 技能学习/升级界面

**相关协议**:
- `C2SLearnSkill` - 学习技能
- `C2SUpgradeSkill` - 升级技能
- `S2CLearnSkillResult` - 学习技能结果
- `S2CUpgradeSkillResult` - 升级技能结果

#### 3.5 自动战斗AI（可选）
- **功能**: 客户端自动战斗逻辑
- **实现**:
  - 自动选择目标
  - 自动释放技能
  - 自动拾取掉落物
  - 自动使用药水（HP/MP低于阈值）

**注意**: 自动战斗AI在客户端实现，但所有操作都需要发送到服务端进行校验

---

### 4. UI层（UI）

#### 4.1 登录界面（LoginUI）
- **功能**: 账号注册和登录
- **界面元素**:
  - 账号输入框
  - 密码输入框
  - 注册按钮
  - 登录按钮
  - 记住账号选项

**相关协议**:
- `C2SRegister` - 账号注册
- `C2SLogin` - 账号登录
- `S2CRegisterResult` - 注册结果
- `S2CLoginResult` - 登录结果

#### 4.2 角色选择界面（RoleSelectUI）
- **功能**: 角色列表显示和创建
- **界面元素**:
  - 角色列表（显示角色信息：名称、职业、等级等）
  - 创建角色按钮
  - 删除角色按钮
  - 进入游戏按钮

**相关协议**:
- `C2SQueryRoles` - 查询角色列表
- `C2SCreateRole` - 创建角色
- `C2SEnterGame` - 进入游戏
- `S2CRoleList` - 角色列表
- `S2CCreateRoleResult` - 创建角色结果
- `S2CLoginSuccess` - 登录成功

#### 4.3 主界面（MainUI）
- **功能**: 游戏主界面（HUD）
- **界面元素**:
  - 角色信息（头像、等级、HP/MP条）
  - 快捷栏（技能、物品）
  - 任务追踪（当前任务列表、进度）
  - 小地图
  - 系统按钮（背包、装备、技能、任务、邮件、设置等）

#### 4.4 背包界面（BagUI）
- **功能**: 物品管理
- **界面元素**:
  - 背包格子（显示物品图标、数量、品质等）
  - 物品详情（悬停显示物品信息）
  - 物品操作（使用、装备、回收等）
  - 背包整理按钮（客户端排序）

**相关协议**:
- `C2SOpenBag` - 查询背包
- `C2SUseItem` - 使用物品
- `C2SRecycleItem` - 回收物品
- `S2CBagData` - 背包数据
- `S2CUpdateBagData` - 更新背包数据
- `S2CUseItemResult` - 使用物品结果
- `S2CRecycleItemResult` - 回收物品结果

#### 4.5 装备界面（EquipUI）
- **功能**: 装备管理
- **界面元素**:
  - 装备槽位（武器、防具等）
  - 装备详情（属性、强化等级等）
  - 装备穿脱按钮
  - 装备强化/精炼/附魔界面

**相关协议**:
- `C2SEquipItem` - 穿戴装备
- `S2CEquipResult` - 穿戴装备结果

#### 4.6 任务界面（QuestUI）
- **功能**: 任务管理
- **界面元素**:
  - 任务列表（进行中、可接取、已完成）
  - 任务详情（目标、进度、奖励）
  - 任务追踪（自动导航到目标位置）
  - 任务接取/提交按钮

**相关协议**:
- `C2STalkToNPC` - 和NPC对话
- `S2CQuestData` - 任务数据
- `S2CTalkToNPCResult` - NPC对话结果

#### 4.7 技能界面（SkillUI）
- **功能**: 技能管理
- **界面元素**:
  - 技能列表（显示技能图标、等级、CD等）
  - 技能详情（伤害、消耗、描述等）
  - 技能学习/升级按钮

#### 4.8 邮件界面（MailUI）
- **功能**: 邮件管理
- **界面元素**:
  - 邮件列表（未读、已读、已领取）
  - 邮件详情（标题、内容、附件）
  - 邮件操作（读取、领取附件、删除）

**相关协议**:
- `S2CMailList` - 邮件列表
- `S2CMailDetail` - 邮件详情

#### 4.9 商城界面（ShopUI）
- **功能**: 商城购买
- **界面元素**:
  - 商品列表（显示物品、价格）
  - 购买按钮
  - 货币显示

**相关协议**:
- `C2SShopBuy` - 商城购买
- `C2SOpenMoney` - 查询货币
- `S2CShopBuyResult` - 商城购买结果
- `S2CMoneyData` - 货币数据

#### 4.10 副本界面（DungeonUI）
- **功能**: 副本选择和管理
- **界面元素**:
  - 副本列表（显示副本信息、难度、奖励预览）
  - 副本推荐（根据玩家等级推荐）
  - 进入副本按钮
  - 副本结算界面（显示用时、击杀数、获得奖励等）

**相关协议**:
- `C2SEnterDungeon` - 进入副本
- `C2SChangeScene` - 切换场景
- `S2CEnterDungeonResult` - 进入副本结果
- `S2CChangeSceneResult` - 切换场景结果

#### 4.11 设置界面（SettingsUI）
- **功能**: 游戏设置
- **界面元素**:
  - 音量设置
  - 画面设置
  - 按键设置
  - 自动战斗设置（开启/关闭自动战斗）

---

### 5. 数据层（Data）

#### 5.1 配置表管理
- **功能**: 加载和管理客户端配置表
- **实现**:
  - JSON配置表加载
  - 配置表缓存
  - 配置表热更新（可选）

#### 5.2 本地数据存储
- **功能**: 存储本地数据（账号信息、设置等）
- **实现**:
  - 使用Godot的`ConfigFile`或自定义JSON存储
  - 账号信息加密存储
  - 设置数据持久化

#### 5.3 玩家数据缓存
- **功能**: 缓存玩家数据，减少网络请求
- **实现**:
  - 角色数据缓存
  - 背包数据缓存
  - 任务数据缓存
  - 数据同步机制

---

## 🎯 开发优先级

### Phase 1: 基础框架（必须优先完成）

1. **网络层** ⚠️ **最高优先级**
   - 网络连接管理
   - Protocol Buffer协议解析
   - 消息发送/接收框架

2. **协议生成工具** ⚠️ **最高优先级**
   - 从proto文件生成C#代码
   - 协议消息映射

3. **登录流程** ⚠️ **最高优先级**
   - 登录界面
   - 账号注册/登录
   - 角色选择界面

### Phase 2: 核心游戏功能

4. **场景系统** ⚠️ **高优先级**
   - 场景管理
   - 实体管理
   - 场景渲染

5. **移动系统** ⚠️ **高优先级**
   - 玩家移动控制
   - 移动同步

6. **战斗系统** ⚠️ **高优先级**
   - 技能释放
   - 伤害显示
   - 战斗动画（与服务端帧定义和时间同步对齐）

7. **时间同步与动画同步** ⚠️ **高优先级**
   - 基于 Ping/Pong 的时间同步模块（Network 层），定期向 Gateway 请求服务器时间并计算本地时间偏移
   - 所有战斗与移动动画使用服务器时间戳驱动（而非本地时钟），减小多端差异
   - 实现技能帧时间线（前摇→判定→后摇）的本地表现层封装，伤害判定严格以服务器结果 `S2CSkillCastResult` 为准

7. **UI系统** ⚠️ **高优先级**
   - 主界面（HUD）
   - 背包界面
   - 装备界面

### Phase 3: 游戏系统功能

8. **任务系统** ⚠️ **中优先级**
   - 任务界面
   - 任务追踪
   - 任务导航

9. **技能系统** ⚠️ **中优先级**
   - 技能界面
   - 技能学习/升级

10. **邮件系统** ⚠️ **中优先级**
    - 邮件界面
    - 邮件操作

11. **商城系统** ⚠️ **中优先级**
    - 商城界面
    - 购买功能

12. **副本系统** ⚠️ **中优先级**
    - 副本选择界面
    - 副本结算界面

### Phase 4: 优化功能

13. **自动战斗AI** ⚠️ **低优先级**
    - 自动战斗逻辑
    - 自动拾取
    - 自动使用药水

14. **背包整理** ⚠️ **低优先级**
    - 一键整理功能

15. **快捷栏** ⚠️ **低优先级**
    - 快捷栏UI
    - 快捷栏配置

---

## 🔧 开发注意事项与架构决策

### 架构原则

1. **模块化设计**: 各个模块独立，通过接口通信
2. **事件驱动**: 使用事件系统解耦模块间通信
3. **单例模式**: 网络管理器、场景管理器等使用单例模式
4. **资源管理**: 合理管理游戏资源，避免内存泄漏

### 网络通信

1. **异步处理**: 所有网络操作使用异步方式，避免阻塞主线程
2. **消息队列**: 使用消息队列管理网络消息（MessageReceiver在主线程处理消息队列）
3. **断线重连**: 实现完善的断线重连机制（最多重试5次，间隔3秒）
4. **心跳机制**: 定期发送心跳包（每5秒），保持连接活跃，消息类型0x06
5. **线程安全**: 消息接收在后台线程，处理在主线程，通过消息队列保证线程安全
6. **SessionId管理**: 客户端发送消息时SessionId可为空（登录前），Gateway会自动创建Session

### 性能优化

1. **对象池**: 对于频繁创建/销毁的对象（如伤害数字、特效），使用对象池
2. **资源预加载**: 预加载常用资源，减少运行时加载
3. **批量渲染**: 合并相同类型的渲染，减少DrawCall
4. **LOD系统**: 根据距离使用不同精度的模型/贴图

### 协议处理

1. **协议验证**: 接收协议后先验证数据合法性
2. **错误处理**: 完善的错误处理和提示
3. **协议缓存**: 对于频繁的协议，可以考虑客户端缓存

### UI开发

1. **响应式设计**: UI适配不同分辨率
2. **UI复用**: 相同类型的UI组件复用
3. **动画优化**: UI动画使用Godot的Tween系统，性能更好

### Protocol Buffer集成

1. **协议生成**: 从`proto/csproto/`生成C#代码，使用`proto/genproto_csharp.bat`脚本
2. **版本管理**: 确保客户端和服务端使用相同版本的协议
3. **协议更新**: 协议更新后需要重新生成代码，并在`ProtocolHandler.InitializeProtocolMaps()`中注册新协议
4. **消息格式**: 
   - 服务端消息格式：`[4字节长度][1字节类型][1字节flags][消息体]`
   - 客户端消息格式：`[2字节MsgId][protobuf数据]`，包装在通用Message中（包含SessionId）
   - 心跳消息类型：0x06 (MsgTypeHeartbeat)，内容为"ping"

---

## 🧪 调试/运行场景教程（供新成员参考）

为了在未完成整条登录链路前快速验证单个场景或功能，可按以下方式操作：

### 1. 直接运行当前场景（推荐）
1. 在 Godot 编辑器中打开目标场景（例如 `client/playground.tscn` 或 `client/Scenes/player.tscn`）。
2. 按 **F6 (Run Current Scene)** 即可独立运行当前场景，不受 `run/main_scene` 限制。
3. 适合调试某个 UI/角色/功能组件。

### 2. 临时修改项目主场景
1. 打开 `client/project.godot`。
2. 在 `[application]` 段落中将 `run/main_scene` 改为需要调试的场景路径：
   ```
   run/main_scene="res://playground.tscn"
   ```
3. 完成调试后，记得改回 `res://Scenes/Login.tscn` 以恢复正常流程。

### 3. 暂时注释登录流程的自动逻辑
如果需要在登录/角色选择 UI 中阻断自动跳转或网络请求：
- 在 `client/Scripts/UI/LoginUI.cs`：
  - 可注释 `_Ready()` 中的 `AutoConnect()`，避免自动连服务器。
  - 可暂时注释登录成功时调用的 `SwitchToRoleSelect()`（`GetTree().ChangeSceneToFile(...)`），防止跳转。
- 在 `client/Scripts/UI/RoleSelectUI.cs`：
  - 若还没有游戏主场景，可以保持 `OnLoginSuccess` 里打印调试信息，暂不切换到主场景。

> ⚠️ 注意：这些注释只应在调试阶段使用，完成功能联调前务必恢复，确保最终流程能从 Login → RoleSelect → 进入游戏。

---

## 📖 客户端脚本开发流程

### 1. Protocol Buffer协议生成与注册流程

#### 步骤1: 生成协议代码
```bash
# Windows
cd proto
.\genproto_csharp.bat

# 生成后的文件位于: client/Scripts/Protocol/
# 主要文件: Cs.cs (C2S协议), Sc.cs (S2C协议), Base.cs (基础数据结构)
```

#### 步骤2: 在ProtocolHandler中注册协议映射
**位置**: `client/Scripts/Network/ProtocolHandler.cs` 的 `InitializeProtocolMaps()` 方法

**C2S协议注册**（客户端发送到服务端）:
```csharp
RegisterC2SProtocol((int)C2SProtocol.C2Sregister, typeof(C2SRegisterReq));
RegisterC2SProtocol((int)C2SProtocol.C2Slogin, typeof(C2SLoginReq));
// ... 其他C2S协议
```

**S2C协议注册**（服务端发送到客户端）:
```csharp
RegisterS2CProtocol((int)S2CProtocol.S2CregisterResult, typeof(S2CRegisterResultReq));
RegisterS2CProtocol((int)S2CProtocol.S2CloginResult, typeof(S2CLoginResultReq));
// ... 其他S2C协议
```

**注意**: 
- 协议ID使用枚举 `C2SProtocol` 和 `S2CProtocol`（定义在生成的 `Cs.cs` 和 `Sc.cs` 中）
- 消息类型使用生成的protobuf类（如 `C2SRegisterReq`、`S2CRegisterResultReq`）

#### 步骤3: 在UI脚本中注册协议处理器
**位置**: UI脚本的 `_Ready()` 方法中

**示例**:
```csharp
public override void _Ready()
{
    // 注册协议处理器
    MessageReceiver.Instance?.RegisterHandler((int)S2CProtocol.S2CloginResult, OnLoginResult);
    MessageReceiver.Instance?.RegisterHandler((int)S2CProtocol.S2Cerror, OnError);
}

// 处理协议消息
private void OnLoginResult(Google.Protobuf.IMessage message)
{
    if (message is S2CLoginResultReq result)
    {
        // 处理登录结果
    }
}
```

**重要**: 在 `_ExitTree()` 中取消注册，避免内存泄漏：
```csharp
public override void _ExitTree()
{
    MessageReceiver.Instance?.UnregisterHandler((int)S2CProtocol.S2CloginResult);
    base._ExitTree();
}
```

#### 步骤4: 发送协议消息
**使用MessageSender发送消息**:
```csharp
// 创建请求消息
var req = new C2SLoginReq
{
    Username = username,
    Password = password
};

// 发送消息（协议ID使用枚举）
MessageSender.Send(req, (int)C2SProtocol.C2Slogin);
```

**完整流程**:
1. 用户操作 → UI事件触发
2. 创建protobuf消息对象
3. 调用 `MessageSender.Send()` 发送
4. 服务端处理并返回响应
5. `MessageReceiver` 自动分发到注册的处理器
6. 在处理器中处理结果并更新UI

### 2. 场景切换机制

#### Login场景切换到RoleSelect场景

**实现方式**:
```csharp
// 在LoginUI.cs中
private void SwitchToRoleSelect()
{
    GetTree().ChangeSceneToFile("res://Scenes/RoleSelect.tscn");
}
```

**场景切换流程**:
1. Godot的 `GetTree().ChangeSceneToFile()` 会：
   - 卸载当前场景（Login.tscn）
   - 加载新场景（RoleSelect.tscn）
   - 调用旧场景的 `_ExitTree()`（此时应取消注册协议处理器）
   - 调用新场景的 `_Ready()`（此时应注册协议处理器）

2. **AutoLoad场景保持**: `AutoLoad.tscn` 中的 `NetworkManager` 和 `MessageReceiver` 是自动加载的，不会因为场景切换而销毁

3. **协议处理器管理**:
   - 每个场景在 `_Ready()` 中注册自己需要的协议处理器
   - 在 `_ExitTree()` 中取消注册，避免内存泄漏和重复处理

### 3. 客户端框架完善清单

#### ✅ 已完成的基础框架
- [x] 网络层（NetworkManager、ProtocolHandler、MessageSender、MessageReceiver）
- [x] 协议生成工具和协议映射注册机制
- [x] 登录流程（LoginUI、RoleSelectUI）
- [x] 本地数据存储（LocalStorage）
- [x] 日志工具（Logger）

#### ⚠️ 待完善的核心框架

**3.1 场景管理系统**（高优先级）
- [ ] `SceneManager.cs` - 场景管理器
  - 场景加载/卸载
  - 场景切换动画
  - 场景配置读取
  - 场景状态管理

**3.2 实体管理系统**（高优先级）
- [ ] `EntityManager.cs` - 实体管理器
- [ ] `Entity.cs` - 基础实体类
- [ ] `PlayerEntity.cs` - 玩家实体
- [ ] `MonsterEntity.cs` - 怪物实体
- [ ] `NPCEntity.cs` - NPC实体
- [ ] `DropItemEntity.cs` - 掉落物实体
- [ ] AOI视野管理

**3.3 数据层框架**（中优先级）
- [ ] `ConfigManager.cs` - 配置表管理器
- [ ] `PlayerDataCache.cs` - 玩家数据缓存

**3.4 工具层框架**（中优先级）
- [ ] `ResourceManager.cs` - 资源管理器
- [ ] `UtilityFunctions.cs` - 工具函数

**3.5 时间同步模块**（高优先级）
- [ ] 在 `NetworkManager.cs` 中实现 Ping/Pong 时间同步
- [ ] 提供统一的"服务器时间"访问接口

**3.6 游戏逻辑框架**（高优先级）
- [ ] `PlayerController.cs` - 玩家控制器
- [ ] `MovementSystem.cs` - 移动系统（集成服务器时间同步）
- [ ] `CombatSystem.cs` - 战斗系统（集成服务器时间同步和帧定义）
- [ ] `SkillSystem.cs` - 技能系统

### 4. 开发新功能的标准流程

1. **协议定义**（如需要）
   - 在 `proto/csproto/` 中定义或修改 `.proto` 文件
   - 运行 `proto/genproto_csharp.bat` 生成C#代码

2. **协议注册**
   - 在 `ProtocolHandler.InitializeProtocolMaps()` 中注册新协议

3. **UI开发**（如需要）
   - 在Godot编辑器中创建场景文件（`.tscn`）
   - 创建对应的C#脚本（如 `XXXUI.cs`）
   - 在脚本的 `_Ready()` 中注册协议处理器
   - 在脚本的 `_ExitTree()` 中取消注册协议处理器

4. **业务逻辑实现**
   - 实现发送消息逻辑（使用 `MessageSender.Send()`）
   - 实现接收消息处理（在注册的处理器中）
   - 实现UI更新逻辑

5. **测试和调试**
   - 测试协议收发
   - 测试UI交互
   - 检查内存泄漏

6. **文档更新**
   - 更新 `docs/godot客户端开发进度文档.md`
   - 记录实现细节和关键代码位置

---

## 📋 协议列表

### 客户端到服务端协议（C2S）

| 协议ID | 协议名 | 说明 |
|--------|--------|------|
| 1 | C2SRegister | 账号注册 |
| 2 | C2SLogin | 账号登录 |
| 3 | C2SVerify | 验证请求 |
| 4 | C2SQueryRoles | 查询角色列表 |
| 5 | C2SCreateRole | 创建角色 |
| 6 | C2SEnterGame | 进入游戏 |
| 7 | C2SReconnect | 重连请求 |
| 8 | C2SUseSkill | 使用技能 |
| 9 | C2SStartMove | 开始移动 |
| 10 | C2SUpdateMove | 移动中更新 |
| 11 | C2SEndMove | 结束移动 |
| 12 | C2SOpenBag | 查询背包 |
| 13 | C2SOpenMoney | 查询货币 |
| 14 | C2SShopBuy | 商城购买 |
| 15 | C2SEquipItem | 穿戴装备 |
| 16 | C2SGMCommand | GM命令 |
| 17 | C2SUseItem | 使用物品 |
| 18 | C2SRecycleItem | 回收物品 |
| 19 | C2STalkToNPC | 和NPC对话 |
| 20 | C2SLearnSkill | 学习技能 |
| 21 | C2SUpgradeSkill | 升级技能 |
| 22 | C2SChangeScene | 切换场景 |
| 23 | C2SPickupItem | 拾取掉落物 |
| 24 | C2SEnterDungeon | 进入副本 |
| 25 | C2SClaimOfflineReward | 领取离线收益 |
| 26 | C2SRevive | 角色复活 |
| 27 | C2SGetNearestMonster | 获取最近怪物 |

### 服务端到客户端协议（S2C）

| 协议ID | 协议名 | 说明 |
|--------|--------|------|
| 0 | S2CError | 错误消息 |
| 1 | S2CRegisterResult | 注册结果 |
| 2 | S2CLoginResult | 登录结果 |
| 3 | S2CRoleList | 角色列表 |
| 4 | S2CCreateRoleResult | 创建角色结果 |
| 5 | S2CEnterScene | 进入场景 |
| 6 | S2CUpdateBagData | 更新背包数据 |
| 7 | S2CQuestData | 任务数据 |
| 8 | S2CLevelData | 等级数据 |
| 9 | S2CBagData | 背包数据 |
| 10 | S2CVipData | VIP数据 |
| 11 | S2CMoneyData | 货币数据 |
| 12 | S2CAttrData | 属性数据 |
| 13 | S2CMailList | 邮件列表 |
| 14 | S2CMailDetail | 邮件详情 |
| 15 | S2CReconnectSuccess | 重连成功 |
| 16 | S2CLoginSuccess | 登录成功 |
| 17 | S2CEntityMove | 实体移动广播 |
| 18 | S2CEntityStopMove | 实体停止移动 |
| 19 | S2CSkillCastResult | 技能释放结果 |
| 20 | S2CEntityAppear | 实体进入视野 |
| 21 | S2CEntityDisappear | 实体离开视野 |
| 22 | S2CShopBuyResult | 商城购买结果 |
| 23 | S2CEquipResult | 穿戴装备结果 |
| 24 | S2CSystemNotification | 系统通知 |
| 25 | S2CGMCommandResult | GM命令结果 |
| 26 | S2CUseItemResult | 使用物品结果 |
| 27 | S2CRecycleItemResult | 回收物品结果 |
| 28 | S2CTalkToNPCResult | NPC对话结果 |
| 29 | S2CLearnSkillResult | 学习技能结果 |
| 30 | S2CUpgradeSkillResult | 升级技能结果 |
| 31 | S2CChangeSceneResult | 切换场景结果 |
| 32 | S2CPickupItemResult | 拾取掉落物结果 |
| 33 | S2CEnterDungeonResult | 进入副本结果 |
| 34 | S2CClaimOfflineRewardResult | 领取离线收益结果 |
| 35 | S2CReviveResult | 角色复活结果 |
| 36 | S2CGetNearestMonsterResult | 获取最近怪物结果 |

---

## 🔗 相关文档

- `docs/服务端开发进度文档.md` - 服务端开发进度文档
- `docs/服务器规划.md` - 服务器规划文档
- `docs/游戏流程优化建议.md` - 游戏流程优化建议
- `docs/godot客户端开发进度文档.md` - 客户端开发进度文档

---

## 💡 提示

这份文档提供了：
- 客户端架构设计
- 核心模块规划
- 技术栈选择
- 目录结构规划
- 开发优先级建议
- 开发注意事项
- 协议列表

**建议**: 查看 `docs/godot客户端开发进度文档.md` 获取详细的开发进度和实现细节。
